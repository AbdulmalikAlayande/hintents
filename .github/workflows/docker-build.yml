name: Docker Multi-Architecture Build

on:
	workflow_dispatch:

jobs:
	docker-build-disabled:
		name: Docker Build Disabled
		runs-on: ubuntu-latest
		steps:
			- name: Explain status
				run: |
					echo "Docker multi-arch build workflow is intentionally disabled."
					echo "Enable push/pull_request triggers and full build steps when needed."
